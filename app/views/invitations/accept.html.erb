<%# ▼▼▼ 1. 背景レイヤー ▼▼▼ %>
<div class="fixed inset-0 bg-gradient-to-br from-purple-600 to-blue-500 z-0"></div>

<%# ▼▼▼ 2. コンテンツレイヤー ▼▼▼ %>
<div class="relative z-10 min-h-screen flex flex-col justify-center py-12 sm:px-6 lg:px-8">
  
  <%# ロゴとアプリ名 %>
  <div class="sm:mx-auto sm:w-full sm:max-w-md text-center mb-6">
    <div class="flex justify-center items-center gap-3 mb-2">
      <%= image_tag "logo.png", class: "h-12 w-auto object-contain", alt: "TripConcierge Logo" %>
      <span class="text-3xl font-bold text-white tracking-tight">TripConcierge</span>
    </div>
  </div>

  <%# タイトルエリア %>
  <div class="sm:mx-auto sm:w-full sm:max-w-md">
    <h2 class="mt-2 text-center text-3xl font-extrabold text-white">
      招待を受け取る
    </h2>
    <p class="mt-2 text-center text-sm text-gray-100">
      以下の旅程への招待が届いています
    </p>
  </div>

  <%# カード部分 %>
  <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
    <div class="bg-white py-8 px-4 shadow-xl sm:rounded-lg sm:px-10">
      
      <%# エラーメッセージ表示エリア %>
      <% if flash[:alert] %>
        <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-6">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
              </svg>
            </div>
            <div class="ml-3">
              <p class="text-sm text-red-700">
                <%= flash[:alert] %>
              </p>
            </div>
          </div>
        </div>
      <% end %>

      <%# 旅程情報 %>
      <div class="bg-blue-50 border border-blue-100 rounded-lg p-4 mb-6 text-center">
        <h3 class="text-lg font-bold text-gray-800"><%= @invitation.trip.title %></h3>
        <p class="text-sm text-gray-500 mt-1">
          権限: <span class="font-bold"><%= @invitation.role == 'editor' ? '編集者' : '閲覧者' %></span>
        </p>
      </div>

      <div class="space-y-4">
        
        <%# 1. ログイン/参加ボタン (そのまま) %>
        <% if user_signed_in? %>
          <%= button_to join_invitation_path(@invitation.token), method: :post, class: "w-full flex justify-center py-3 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 transition cursor-pointer" do %>
            参加を確定する
          <% end %>
          <p class="text-xs text-center text-gray-500 mt-1">
            ログイン中: <%= current_user.email %>
          </p>

        <% else %>
          <%= link_to new_user_session_path, class: "w-full flex justify-center py-3 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition" do %>
            ログイン / 新規登録して参加
          <% end %>
          <p class="text-xs text-center text-gray-400 mt-2">
            ※ログイン後に自動的にこの画面に戻ります
          </p>
        <% end %>

        <div class="relative py-2">
          <div class="absolute inset-0 flex items-center">
            <div class="w-full border-t border-gray-300"></div>
          </div>
          <div class="relative flex justify-center text-sm">
            <span class="px-2 bg-white text-gray-500">または</span>
          </div>
        </div>
        
        <%= button_to accept_guest_invitation_path(token: @invitation.token), method: :post, class: "cursor-pointer w-full flex justify-center py-3 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition" do %>
          登録せずにゲストとして閲覧
        <% end %>
        
        <%# 編集者権限でゲスト参加する場合の注記 %>
        <% if @invitation.role == 'editor' %>
          <p class="text-xs text-center text-orange-600 mt-2">
            ※ゲスト参加の場合、編集権限があっても<strong>閲覧のみ</strong>となります。<br>
            編集するにはログインして参加してください。
          </p>
        <% else %>
          <p class="text-xs text-center text-gray-400 mt-2">
            ※ゲスト閲覧はブラウザを閉じると終了します
          </p>
        <% end %>
      </div>

    </div>
  </div>
</div>